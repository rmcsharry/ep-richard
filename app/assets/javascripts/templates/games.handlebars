<ul class="games-list">
{{#each}}
  <li class="games-list__game">
    {{#link-to 'game' this}}
      <img {{bind-attr src=image_url}}/>
      <div class="name">{{name}}</div>
      <div class="description">{{description}}</div>
    {{/link-to}}
  </li>
{{/each}}
</ul>
