<div class="row">
  <div class="col-md-4">
    <%= form_for [:admin, @game], html: {role: "form"} do |f| %>
      <%= render 'shared/error_messages', object: f.object %>
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, placeholder: "", class: "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :description %>
        <%= f.text_field :description, placeholder: "", class: "form-control", rows: 3 %>
        <small>Keep this very short!</small>
      </div>
      <div class="form-group">
        <%= f.label :eyfs_area, "EYFS Area" %>
        <%= f.select :eyfs_area, Game::EYFS_AREAS, {include_blank: true}, class: "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :eyfs_goal, "Learning Goal" %>
        <%= f.select :eyfs_goal, Game::EYFS_GOALS, {include_blank: true}, class: "form-control" %>        
      </div>
      <div class="form-group">
        <%= f.label :did_you_know_fact %>
        <%= f.text_field :did_you_know_fact, placeholder: "", class: "form-control", rows: 7 %>
        <small>This is the key fact about how the game benefits children. It appears before the instructions and is also sent as an SMS to parents. Keep it to one sentence.</small>
      </div>
      <div class="form-group">
        <%= f.label :instructions %>
        <%= f.text_area :instructions, placeholder: "", class: "form-control", rows: 7 %>
        <small>This is the text that appears next to the video on the game page. You can use <a href="/markdown.html" target="_blank">Markdown</a> to format your instructions</small>
      </div>
      <div class="form-group">
        <%= f.label :top_tip %>
        <%= f.text_field :top_tip, placeholder: "", class: "form-control", rows: 7 %>
        <small>This is a handy tip to help parents. This will appear after the instructions. Again, keep it to one sentence.</small>
      </div>
      <div class="form-group">
        <%= f.label :position %>
        <%= f.text_field :position, placeholder: "", class: "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :in_default_set, "Add to default set" %>
        <%= f.check_box :in_default_set, class: "form-control" %>
      </div>
      <div class="form-group">
        <%= f.label :video_url, "Video URL" %>
        <%= f.text_field :video_url, placeholder: "", class: "form-control" %>
      </div>
      <div class="form-group">
        <% if params[:action] == 'edit' || params[:action] == 'update' %>
          <%= f.submit "Update game", class: "btn btn-primary" %>
        <% else %>
          <%= f.submit "Add game", class: "btn btn-primary" %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>